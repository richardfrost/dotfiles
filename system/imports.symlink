# Import functions
. "$HOME/.functions"

# Import variables
. "$HOME/.vars"

# Import aliases
. "$HOME/.aliases"

# Import docker aliases and functions
. "$HOME/.docker_aliases"

# Import any custom vars/functions
if [[ -f "$HOME/.custom" ]]; then
  . "$HOME/.custom"
fi
