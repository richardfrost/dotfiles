# Import variables
. "$HOME/.vars"

# Import functions
. "$HOME/.functions"

# Import aliases
. "$HOME/.aliases"

# Import docker aliases and functions
. "$HOME/.docker_aliases"

# Import any custom vars/functions
if [[ -f "$HOME/.custom" ]]; then
  . "$HOME/.custom"
fi
